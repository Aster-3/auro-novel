import { Text, View } from "react-native";
import { Screen } from "../components/layout/Screen";

const NotificationScreen = () => {
  return (
    <Screen>
      <Text>Placeholder for NotificationScreen</Text>
    </Screen>
  );
};
export default NotificationScreen;
